package com.notify.notify_beta.Listeners;

public interface OnRecycleItemClickedListener {

    //CustomListener interface to get objects when user (long)clicks on a recyclerView item

    void onItemOfListClicked(Object o);
    void onItemLongClicked(Object o);
}
